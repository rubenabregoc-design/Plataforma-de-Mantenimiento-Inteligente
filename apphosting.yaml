# Mantech Pro App Hosting Configuration
runtime: nodejs20

runConfig:
  concurrency: 80
  cpu: 1
  memoryMiB: 512

# Las variables de entorno BREVO_API_KEY y SENDER_EMAIL 
# se gestionan directamente desde la Consola de Firebase (Sección Entorno)
